// Job type enum
export enum JobType {
  Remote = "remote",
  Onsite = "onsite",
  Hybrid = "hybrid",
}
// Job status enum
export enum JobStatus {
  Open = "open",
  Closed = "closed",
  Draft = "draft",
}

// Job schedule enum
export enum JobSchedule {
  FullTime = "full_time",
  PartTime = "part_time",
  Contract = "contract",
  Internship = "internship",
}

// Email Status enum
export enum EmailStatus {
  Sent = "sent",
  Failed = "failed",
  Pending = "pending",
}
